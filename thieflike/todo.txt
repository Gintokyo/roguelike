# TODO
        # Other effects (poison, confusion, etc.)
        #elif effect.startswith("poison"):
        #    # Splits the words "poison 3" -> ["poison", "3"]
        #    parts = effect.split()
        #    if effect == "attack":
        #        enemy.stats["HP"] -= self.power
        #        print(f"{self.name} does {self.power} damage to {enemy.name}'s HP")
        #        print(f"{enemy.name}'s HP: {enemy.stats["HP"]}")
        #    elif effect == "heal":
        #        enemy.stats["HP"] += self.power
        #        print(f"{self.name} heals {self.power} HPs to {enemy.name}")
        #        print(f"{enemy.name}'s HP: {enemy.stats["HP"]}")